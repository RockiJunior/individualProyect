const axios = require('axios');
const funcion = async () => {
	// let response = await axios.get(
	// 	`${API_URL}api/games?key=${API_KEY}`
	// );
	// let array = [];
	// for (let i = 0; i < 5; i++) {
	// 	response = await axios.get(response.data.next);
	// 	array = [
	// 		...array,
	// 		{
	// 			page: i + 1,
	// 			games: response.data.results,
	// 		},
	// 	];
	// }
	console.log(isNaN('3498sd'));
};
funcion();
